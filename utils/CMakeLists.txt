PROJECT(run-utils)

FILE(GLOB_RECURSE CPP_FILES ${PROJECT_SOURCE_DIR}/*.cpp ${PROJECT_SOURCE_DIR}/*.hpp)

ADD_LIBRARY(${PROJECT_NAME} STATIC ${CPP_FILES})

TARGET_COMPILE_FEATURES(${PROJECT_NAME} PRIVATE cxx_std_17)
